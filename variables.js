var fullName="Aishwarya Lakshmi";

var courseName="React JS Bootcamp";

var isLoggedIn=true;//true/false is reserved keywords but not True/False

var loggedCount=20;

// console.log(loggedCount);

var paymentMode;
console.log(paymentMode);

paymentMode="Credit Card";

console.log(paymentMode);



//scope in javascript
//why let keyword. cause in front end part  we need much more lenient part
//that does not throw error immediately so var was given 
//cause var gives undefined instead of error

//functions are scope in js instead of {}
console.log(name);
var name="hittesh";
//let limits scope within the function
if(true){
    let lName="Kumar";
}
console.log(lName);

//template strings


